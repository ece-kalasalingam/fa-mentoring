{
	"name": "api",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --ip localhost --port 8787",
		"start": "wrangler dev --ip localhost --port 8787",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.16.3",
		"typescript": "^5.5.2",
		"vitest": "^4.1.0",
		"wrangler": "^4.90.0"
	},
	"dependencies": {
		"@libsql/client": "^0.17.3"
	}
}
